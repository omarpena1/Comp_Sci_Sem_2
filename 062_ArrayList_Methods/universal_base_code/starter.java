import pkg.*;
import java.util.Scanner;
import java.util.Random;


class starter {
	
	public static void toStringArray(ArrayList<Integer> a){
		int increment = 0;
	
	while(increment < a.size()){
		System.out.println(a.get(increment));
		increment++;
	}
	}
	
	public static integer getArrayListAverage(ArrayList<Integer> a){
		int aggregate = 0;
		int iterate = 0;
		while(iterate < a.size()){
			aggregate = aggregate + a.get(iterate);
			iterate++;
		}
		
		return(aggregate/(a.size()));
	}
	
	public static integer getArrayListMax(ArrayList<Integer> a){
		int iterate = 0;
		int compare = 
		
		while(iterate < a.size()){
		if(a.get(iterate)>compare)
		
			iterate++;
		}	
	}
	
	
	
	public static void main(String args[]) {
	
	Scanner sc = new Scanner(System.in);
	
ArrayList <Integer> value = new ArrayList<Integer>();
int iterate = 0;

	System.out.println("How many values do you want in your ArrayList?");
	int items = sc.nextInt();
	
	while(items>=iterate){
		cookiearr.add("cookie");
		iterate++;
	}
		
	}
}
